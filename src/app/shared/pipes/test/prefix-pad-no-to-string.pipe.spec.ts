import {PrefixPadNoToStringPipe} from '../prefix-pad-no-to-string.pipe';

describe('PrefixPadNoToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new PrefixPadNoToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
